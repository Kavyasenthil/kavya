#include <stdio.h>

int main(void)
{
	int a,b;
	printf("\n enter the 2 numbers");
	scanf("%d %d",&a,&b);
		if(a==b)
		printf("\n %d is a perfect square",a*b);
		else 
		printf("\n %d is not a perfect square",a*b);
	

	return 0;
}
